# План разработки Mission Recorder

## 1. Краткое понимание продукта

Mission Recorder - это локальный кроссплатформенный рекордер экрана "как Bandicam", но бесплатный, открытый и реализованный в Kotlin/KMP-стиле. Продукт должен поддерживать запись всего экрана, выбранного монитора, выделенной области, конкретного окна или приложения, микрофона и системного звука. Дополнительно нужны replay buffer последних `N` минут, экспорт кадров, CLI для автоматизации и Compose Multiplatform GUI для удобного управления.

Функции уровня Bandicam должны быть реализованы в стиле Mission Recorder:

- без обязательного облака;
- без скрытого захвата и неявных разрешений;
- без платных ограничений на базовые сценарии;
- с локальными файлами, понятными настройками и открытой архитектурой;
- с явным разделением общей логики и platform-specific API.

## 2. Аудит текущего репозитория

Текущая структура минимальная:

- `app` - JVM application entry point;
- `utils` - shared JVM-модуль с демонстрационным `Printer`;
- `buildSrc` - convention plugin `buildsrc.convention.kotlin-jvm`;
- `gradle/libs.versions.toml` - version catalog;
- `README.md` - описание целевого продукта;
- `AGENTS.md` - инженерные правила проекта.

Что уже есть:

- Gradle Wrapper;
- multi-project build с `:app` и `:utils`;
- Kotlin JVM convention plugin;
- зависимости `kotlinx-datetime`, `kotlinx-serialization-json`, `kotlinx-coroutines-core`;
- базовый тест в `utils`.

Что не реализовано:

- нет capture domain model;
- нет pipeline записи;
- нет CLI-команд продукта;
- нет Compose Multiplatform;
- нет KMP source sets;
- нет encoder/muxer слоя;
- нет audio слоя;
- нет platform adapters;
- нет настроек продукта;
- нет hotkeys, replay buffer и export frames;
- нет документации по разрешениям и native ownership.

Текущая техническая проблема:

- Gradle запускается на Java 21, а `buildSrc` настроен на JVM toolchain 25. `.\gradlew.bat check` не доходит до тестов из-за требования JVM runtime 25. В Milestone 0 нужно либо настроить окружение на JDK 25, либо временно снизить toolchain до доступного LTS, если это соответствует целям проекта.

## 3. Предлагаемая архитектура

Целевая модульная структура:

- `:capture-core` - платформонезависимая доменная логика записи, модели, state machine, use cases, ошибки, события, Flow-контракты.
- `:capture-platform-api` - интерфейсы платформенных адаптеров: source discovery, video capture, permissions, window/application metadata.
- `:capture-windows` - Windows adapter для screen/window/app capture.
- `:capture-macos` - macOS adapter для ScreenCaptureKit и permissions.
- `:capture-linux` - Linux adapter для PipeWire, xdg-desktop-portal и X11 fallback.
- `:capture-android` - Android adapter для MediaProjection.
- `:audio-core` - общие модели аудио, микширование, уровни, sync metadata, routing.
- `:audio-platform-api` - интерфейсы микрофона, system audio, app audio filtering.
- `:audio-windows`, `:audio-macos`, `:audio-linux`, `:audio-android` - platform audio adapters.
- `:encoder` - интерфейсы encoder/muxer, настройки кодеков, timestamp handling, output writer.
- `:settings` - профили настроек, сериализация, миграции, validation.
- `:replay-buffer` - rolling buffer последних `N` минут, политика памяти/диска, save replay.
- `:export` - экспорт кадров в изображения, naming pattern, contact sheet при необходимости.
- `:hotkeys` - глобальные горячие клавиши через platform adapters.
- `:cli` - CLI-команды, parsing, exit codes, machine-readable output.
- `:compose-ui` - stateless Compose screens, view models, UI state.
- `:app-desktop` - desktop wiring для CLI/GUI на JVM.
- `:app-android` - Android app wiring, permissions flow, foreground service.

Границы ответственности:

- core-модули не знают про JNI, WinAPI, ScreenCaptureKit, PipeWire, MediaProjection, file dialogs или UI;
- platform adapters реализуют только низкоуровневый доступ к устройствам и ОС;
- encoder не принимает UI-модели и не знает про Compose;
- CLI и GUI используют одни и те же use cases;
- production dispatchers и scopes связываются только на application boundary;
- тесты доменной логики используют fake adapters.

## 4. Пайплайн записи

Основной pipeline:

1. Пользователь выбирает `CaptureSource`.
2. Settings validation проверяет источник, регион, аудио, output path, codec и разрешения.
3. Permission service запрашивает или проверяет права платформы.
4. Video adapter создает поток `VideoFrame`.
5. Audio adapters создают потоки `AudioFrame` для микрофона и системного звука.
6. Synchronizer выравнивает аудио и видео по единому clock abstraction.
7. Encoder кодирует video/audio frames.
8. Muxer пишет контейнер в temporary output.
9. После успешного stop файл атомарно перемещается в целевой output.
10. Metrics и final state публикуются через `StateFlow`.

Видео:

- frame timestamp должен быть monotonic;
- нужно хранить размер, pixel format, stride, source id, scale factor;
- dropped frames должны попадать в metrics;
- region/window/app capture должен нормализовать координаты с учетом DPI/scale.

Микрофон:

- выбирать устройство по stable id;
- поддерживать sample rate, channel count, format;
- публиковать уровень сигнала для GUI;
- корректно освобождать device handle при cancel/failure.

Системный звук:

- Windows: WASAPI loopback;
- macOS: ScreenCaptureKit audio там, где применимо, или CoreAudio route;
- Linux: PipeWire/PulseAudio monitor sources;
- Android: AudioPlaybackCapture API для разрешенных приложений.

Синхронизация:

- единый `MediaClock`;
- timestamp normalization на входе adapter слоя;
- drift correction для длинных записей;
- explicit policy для dropped/duplicated frames.

Ошибки и отмена:

- все long-running операции должны быть cancellable;
- cleanup выполняется в `finally`;
- ошибки маппятся в доменные типы: permission denied, source unavailable, encoder failed, disk full, device lost;
- state machine не должна зависать в `Stopping` или `Preparing`.

## 5. План реализации по этапам

### Milestone 0: базовая подготовка

- решить конфликт JDK 21 vs toolchain 25;
- добиться прохождения `.\gradlew.bat check`;
- удалить или изолировать demo-код `Printer`;
- добавить документацию по целевой структуре модулей;
- определить naming conventions для пакетов `io.aequicor`.

Definition of Done:

- Gradle build стабилен;
- `check` проходит локально;
- README и план не противоречат структуре;
- demo-код не считается частью product implementation.

### Milestone 1: capture core

- создать `:capture-core`;
- описать `CaptureSource`, `CaptureRegion`, `RecordingSettings`, `EncoderSettings`, `RecordingState`;
- реализовать state machine записи;
- добавить `RecordingController` с `StateFlow<RecordingState>`;
- добавить fake video/audio adapters;
- покрыть start/stop/cancel/failure unit-тестами.

Definition of Done:

- core не зависит от platform/native/UI;
- state transitions покрыты тестами;
- cancellation тестируется через `kotlinx-coroutines-test`;
- нет `GlobalScope`, hardcoded dispatchers и `Thread.sleep`.

### Milestone 2: MVP video-only recording на Windows

- создать platform API для video capture;
- реализовать Windows prototype для full screen или monitor capture;
- добавить encoder/muxer abstraction;
- записывать короткий MP4/WebM файл без аудио;
- собирать metrics: fps, dropped frames, duration.

Definition of Done:

- запись стартует и останавливается из кода;
- output файл воспроизводится стандартным плеером;
- ресурсы освобождаются при stop и cancel;
- ошибки adapter слоя маппятся в domain errors.

### Milestone 3: CLI MVP

- создать `:cli`;
- добавить команды `list-sources`, `record screen`, `record monitor`;
- добавить параметры output, fps, codec preset, duration;
- определить exit codes;
- добавить tests на command parsing.

Definition of Done:

- запись можно запустить из терминала;
- help понятен;
- ошибки идут в stderr;
- успешный результат возвращает exit code 0.

### Milestone 4: Compose Desktop GUI MVP

- подключить Compose Multiplatform;
- создать главный экран записи;
- вывести source selector, output selector, record/stop, timer, status;
- использовать lapis lazuli primary `#26619C`;
- держать экраны stateless, state hoisting через view models.

Definition of Done:

- GUI запускается на desktop;
- основной сценарий record/stop доступен из UI;
- composables не содержат platform capture logic;
- UI state публикуется через `StateFlow`.

### Milestone 5: region и monitor capture

- добавить выбор монитора;
- добавить capture region model;
- реализовать координаты с учетом DPI/scale;
- добавить CLI `record region`;
- добавить GUI selection overlay.

Definition of Done:

- регион записывается без смещения;
- multi-monitor сценарии проверены;
- invalid region отклоняется validation слоем.

### Milestone 6: microphone audio

- добавить audio source discovery;
- реализовать microphone capture;
- добавить audio encoder path;
- синхронизировать mic audio с video;
- добавить индикатор уровня в GUI.

Definition of Done:

- запись с микрофоном воспроизводится синхронно;
- выбор устройства работает;
- cancel освобождает audio device;
- fake audio adapter покрывает sync tests.

### Milestone 7: system audio

- реализовать system loopback для первой платформы;
- добавить mixed mic + system audio;
- добавить настройки уровней;
- добавить mute/solo controls.

Definition of Done:

- system audio записывается в output файл;
- микрофон и system audio можно включать независимо;
- ошибки permissions/device unavailable понятны пользователю.

### Milestone 8: app audio filtering и app/window capture

- добавить discovery окон и приложений;
- реализовать `record app`;
- добавить app audio filtering там, где есть API;
- определить fallback поведение для платформ без reliable filtering.

Definition of Done:

- окно/приложение записывается без лишнего окружения, где это поддерживается;
- unsupported filtering явно отражается в CLI/GUI;
- fallback не вводит пользователя в заблуждение.

### Milestone 9: replay buffer

- реализовать rolling buffer последних `N` минут;
- добавить memory/disk policy;
- добавить `save-replay`;
- добавить hotkey save replay.

Definition of Done:

- буфер не растет бесконечно;
- сохраняются именно последние `N` минут;
- save работает без остановки фоновой записи;
- edge cases покрыты fake clock tests.

### Milestone 10: export frames

- добавить `export-frames`;
- поддержать frame interval или output fps;
- добавить PNG/JPEG/WebP в зависимости от возможностей;
- добавить GUI экран экспорта.

Definition of Done:

- кадры экспортируются детерминированно;
- naming pattern стабилен;
- ошибки input/output отображаются в CLI и GUI.

### Milestone 11: packaging и релизы

- настроить desktop packaging;
- подготовить Android app packaging;
- добавить release checklist;
- документировать permissions для каждой платформы;
- добавить smoke tests.

Definition of Done:

- есть воспроизводимые артефакты сборки;
- пользователь может установить и запустить приложение;
- README содержит поддерживаемые платформы и ограничения.

## 6. Платформенная стратегия

### Windows

Видео:

- сначала Windows Graphics Capture или DXGI Desktop Duplication;
- для window/app capture использовать Windows Graphics Capture при доступности;
- учитывать DPI awareness и multi-monitor.

Аудио:

- WASAPI для микрофона и loopback;
- app-specific audio filtering рассматривать отдельно через доступные Windows APIs и ограничения ОС.

Разрешения и риски:

- права на экран обычно проще, чем на macOS/Android;
- возможны проблемы с protected content и UAC/elevated windows;
- hardware encoding зависит от GPU и драйверов.

Реализовать первым:

- Windows full screen или monitor video-only MVP.

### macOS

Видео:

- ScreenCaptureKit для screen/window/app capture;
- учитывать Retina scale и permission model.

Аудио:

- ScreenCaptureKit audio для system audio там, где применимо;
- AVAudioEngine/CoreAudio для микрофона.

Разрешения и риски:

- нужны Screen Recording и Microphone permissions;
- пользователь должен явно разрешить доступ в System Settings;
- app capture и audio capture зависят от версии macOS.

Реализовать после Windows MVP:

- screen/window capture с понятным permissions flow.

### Linux Ubuntu

Видео:

- PipeWire + xdg-desktop-portal для Wayland;
- X11 fallback через XCB/FFmpeg/GStreamer при необходимости.

Аудио:

- PipeWire или PulseAudio monitor sources;
- app audio filtering возможен не во всех конфигурациях.

Разрешения и риски:

- разные desktop environments ведут себя по-разному;
- Wayland требует portal flow;
- window capture может быть ограничен compositor-ом.

Реализовать после стабилизации domain/core:

- PipeWire screen capture через portal.

### Android

Видео:

- MediaProjection API;
- foreground service для длительной записи;
- MediaCodec/MediaMuxer для encoding.

Аудио:

- AudioRecord для микрофона;
- AudioPlaybackCapture API для разрешенных приложений и системных ограничений.

Разрешения и риски:

- нужны runtime permissions;
- background ограничения;
- нельзя гарантировать захват звука всех приложений;
- UI/UX permissions flow критичен.

Реализовать отдельным этапом:

- Android app после desktop core и encoder abstractions.

## 7. Модель данных и основные интерфейсы

`CaptureSource`:

- тип источника: screen, monitor, region, window, application;
- stable id источника;
- display name;
- platform metadata в opaque domain-safe форме;
- supported capabilities.

`CaptureRegion`:

- `x`, `y`, `width`, `height`;
- `monitorId`;
- `scaleFactor`;
- coordinate space;
- validation state.

`AudioSource`:

- тип: none, microphone, system loopback, application, mixed;
- device id;
- display name;
- sample rate;
- channel count;
- supported formats.

`RecordingSession`:

- session id;
- settings snapshot;
- startedAt;
- output path;
- current state;
- metrics;
- final result или failure.

`RecordingSettings`:

- capture source;
- audio sources;
- output path;
- frame rate;
- cursor capture flag;
- replay duration;
- encoder settings;
- permission policy.

`EncoderSettings`:

- container;
- video codec;
- audio codec;
- video bitrate;
- audio bitrate;
- keyframe interval;
- hardware acceleration mode;
- pixel format.

`RecordingState`:

- `Idle`;
- `Preparing`;
- `Recording`;
- `Paused`;
- `Stopping`;
- `Completed`;
- `Failed`;
- `Cancelled`.

`ExportSettings`:

- input file;
- output directory;
- image format;
- frame interval или target fps;
- naming pattern;
- overwrite policy.

Ключевые интерфейсы:

- `CaptureSourceRepository` - список доступных screen/monitor/window/app sources;
- `VideoCaptureAdapter` - запуск video frame stream;
- `AudioCaptureAdapter` - запуск audio frame stream;
- `PermissionGateway` - проверка и запрос разрешений;
- `MediaEncoder` - кодирование потоков;
- `RecordingStore` - metadata записей и output paths;
- `HotkeyRegistry` - регистрация глобальных горячих клавиш;
- `Clock` или `MediaClock` - тестируемое время pipeline.

## 8. CLI-план

Команды:

- `mission-recorder list-sources`;
- `mission-recorder list-audio`;
- `mission-recorder record screen --output out.mp4`;
- `mission-recorder record monitor --id 1 --output out.mp4`;
- `mission-recorder record region --x 100 --y 100 --width 1280 --height 720 --output out.mp4`;
- `mission-recorder record app --pid 1234 --output out.mp4`;
- `mission-recorder replay start --duration 5m`;
- `mission-recorder replay save --output replay.mp4`;
- `mission-recorder export-frames --input in.mp4 --fps 1 --output frames`;

Общие опции:

- `--fps`;
- `--video-codec`;
- `--audio-codec`;
- `--video-bitrate`;
- `--audio-bitrate`;
- `--mic`;
- `--system-audio`;
- `--cursor`;
- `--duration`;
- `--json`;
- `--verbose`.

CLI должен:

- возвращать стабильные exit codes;
- поддерживать machine-readable JSON для automation;
- не смешивать diagnostics и structured output;
- валидировать настройки до старта записи;
- корректно обрабатывать Ctrl+C через cancellation.

## 9. GUI-план

Compose Multiplatform экраны:

- главный экран записи;
- выбор источника;
- настройки видео;
- настройки аудио;
- настройки фоновой записи;
- список записей;
- экспорт кадров;
- экран разрешений и диагностики.

Главный экран:

- source selector;
- audio summary;
- encoder preset;
- output path;
- record/stop button;
- timer;
- recording state;
- quick save replay.

Выбор источника:

- tabs или segmented control: screen, monitor, region, window, app;
- список мониторов и окон;
- stable keys для списков;
- preview только после разрешений.

Настройки видео:

- fps;
- resolution policy;
- codec;
- bitrate;
- cursor capture;
- hardware acceleration.

Настройки аудио:

- microphone device;
- system audio toggle;
- app audio filter;
- input level meters;
- per-source gain;
- mute toggles.

Визуальный стиль:

- primary color `#26619C`;
- нейтральные поверхности;
- restrained accents;
- практичный плотный layout;
- без маркетингового hero-first подхода;
- Google Fonts Material Symbols для иконок;
- строки, изображения и шрифты хранить в Compose Multiplatform resources.

Архитектурные правила UI:

- screens stateless;
- state hoisting;
- `StateFlow` для UI state;
- `SharedFlow` для одноразовых events;
- semantics для доступности;
- stable keys в списках;
- expensive recomposition избегать через стабильные модели и derived state.

## 10. Тестовая стратегия

Unit-тесты:

- validation настроек;
- state machine записи;
- session lifecycle;
- domain error mapping;
- output path policy;
- encoder settings compatibility.

Coroutine/Flow tests:

- start/stop events;
- cancellation;
- backpressure;
- dropped frame policy;
- state ordering;
- replay buffer timing.

Rolling buffer tests:

- сохранение последних `N` минут;
- ограничение памяти и диска;
- корректное поведение при save во время записи;
- fake clock вместо реального времени.

Adapter tests:

- fake adapters в unit-тестах;
- integration tests для platform adapters отдельно;
- реальные устройства не использовать в обычных unit-тестах.

GUI tests:

- проверка state rendering;
- disabled states при отсутствии permissions;
- semantics labels;
- screenshot или визуальная проверка для ключевых экранов.

Общие правила:

- `runTest`;
- `StandardTestDispatcher`;
- `advanceUntilIdle`, `runCurrent`, `advanceTimeBy`;
- без `Thread.sleep`;
- без hardcoded dispatchers в business logic.

## 11. Риски и неизвестные

Системный звук:

- API сильно отличаются между Windows, macOS, Linux и Android;
- на Android нельзя гарантировать захват всех приложений;
- на Linux поведение зависит от PipeWire/PulseAudio и desktop environment.

Фильтрация по приложениям:

- не везде доступна надежно;
- может потребовать платформенных fallback-стратегий;
- UI должен честно показывать unsupported state.

Захват окон/приложений:

- protected content может быть недоступен;
- elevated/admin windows на Windows могут вести себя иначе;
- Wayland может ограничивать прямой доступ.

Разрешения:

- macOS требует явного Screen Recording permission;
- Android требует MediaProjection consent и foreground service;
- Linux portal flow зависит от окружения.

Производительность:

- software encoding может перегружать CPU;
- hardware encoding зависит от драйверов;
- multi-monitor/high-DPI/60fps требуют раннего profiling.

Синхронизация:

- audio/video drift на длинных записях;
- dropped frames;
- разные clock sources в platform adapters.

## 12. Definition of Done по крупным направлениям

Core:

- модели стабильны;
- state machine покрыта тестами;
- нет platform leaks;
- cancellation корректна.

Video capture:

- выбранный источник реально записывается;
- размеры и координаты корректны;
- output воспроизводится;
- dropped frames измеряются.

Audio capture:

- mic/system audio записываются отдельно и вместе;
- уровни отображаются;
- sync приемлем на длинных записях;
- device loss обрабатывается.

CLI:

- help, validation, exit codes;
- machine-readable output;
- Ctrl+C корректно завершает запись;
- команды покрыты parsing tests.

GUI:

- основной сценарий record/stop работает;
- нет platform logic в composables;
- состояния ошибок понятны;
- UI соответствует стилю `#26619C` и практичному desktop layout.

Replay buffer:

- буфер ограничен;
- сохраняются последние `N` минут;
- save не ломает ongoing recording;
- есть tests на время и лимиты.

Export frames:

- кадры экспортируются с ожидаемой частотой;
- имена файлов стабильны;
- ошибки input/output понятны.

Release:

- артефакты собираются воспроизводимо;
- permissions задокументированы;
- smoke tests проходят;
- README отражает поддержанные платформы и ограничения.

## 13. Рекомендуемый порядок первых задач

1. Решить JDK/toolchain mismatch: JDK 25 для Gradle runtime или согласованное снижение toolchain.
2. Добиться прохождения `.\gradlew.bat check`.
3. Зафиксировать решение по переходу от JVM-only к KMP/Compose Multiplatform.
4. Создать `:capture-core`.
5. Перенести демонстрационный `utils` за пределы продуктовой логики или удалить позже отдельной задачей.
6. Описать доменные модели записи.
7. Реализовать `RecordingState` и state machine.
8. Добавить fake adapters для видео и аудио.
9. Написать tests на start, stop, cancel, failure.
10. Спроектировать `VideoFrame`, `AudioFrame`, `MediaTimestamp`.
11. Создать `:settings` с validation и serialization.
12. Создать `:encoder` с fake encoder и контрактом muxer/output writer.
13. Создать `:cli` skeleton и `list-sources`.
14. Реализовать первый Windows video-only adapter prototype.
15. Подключить запись output файла через encoder/muxer.
16. Добавить CLI `record screen`.
17. После CLI MVP подключить Compose Desktop shell.
18. Добавить выбор монитора и region capture.
19. Добавить microphone capture.
20. После стабилизации audio/video sync переходить к system audio, replay buffer и app capture.
